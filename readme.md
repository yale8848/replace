# Replace text tool

# Usage

```cmd

replace -c config.json

```

config.json

```json

{
  "replace":[{
    "paths":["test"],
    "regReplace":[
      {
        "regex":"yale8848",
        "replacement":"[yale8848]"
      }
    ],
    "recursive":true,
    "silent":false
  }]
}

```

